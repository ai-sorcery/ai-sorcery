# JetBrains IDE settings
.idea

# macOS Finder metadata
.DS_Store

# Per-repo agent task files (personal / untracked by design)
llm-tasks/
commit-disallowed-terms.txt
docs/superpowers/

# Sandbox for the demoing-sorcery-skills meta-skill — wiped on every run
demo-workspace/
